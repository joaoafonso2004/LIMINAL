# Escape ending video

`END.mp4` is the original ending supplied by the creator. `END.ogv` is its
content-preserving Ogg Theora conversion for direct Godot playback. The game
plays it in full, follows it with five seconds of TV static, and only then
reveals the credits. A cinematic fade fallback remains if the resource is
missing.
